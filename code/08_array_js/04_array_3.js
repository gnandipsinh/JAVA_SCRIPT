// calculating array length

const fruits = [
  "apple",
  "banana",
  "cheery",
  "mango",
  "watermelon",
  "grapes",
  "strawberry",
];

console.log("fruits length: ", fruits.length);

// Increase and Decrease the Array Length

console.log("fruits element and their length before: ", fruits.length, fruits);

// increase length

fruits.length = 10;

console.log(
  "fruits element and its length after increasing: ",
  fruits.length,
  fruits,
);

// decrease length

fruits.length = 4;

console.log(
  "fruits element and its length after decreeing: ",
  fruits.length,
  fruits,
);

//example 2

const colors = ["red", "blue", "green", "yellow", "black"];

console.log("colors length:", colors.length);

console.log("Before:", colors.length, colors);

// Increase length
colors.length = 8;

console.log("After Increasing:", colors.length, colors);

// Decrease length
colors.length = 3;

console.log("After Decreasing:", colors.length, colors);


//example 3'

const cities = ["Ahmedabad", "Surat", "Rajkot", "Bhavnagar", "Vadodara"];

console.log("cities length:", cities.length);

console.log("Before:", cities.length, cities);

// Increase length
cities.length = 7;

console.log("After Increasing:", cities.length, cities);

// Decrease length
cities.length = 2;

console.log("After Decreasing:", cities.length, cities);


// example 4

const animals = ["Lion", "Tiger", "Elephant", "Zebra", "Monkey"];

console.log("animals length:", animals.length);

console.log("Before:", animals.length, animals);

// Increase length
animals.length = 9;

console.log("After Increasing:", animals.length, animals);

// Decrease length
animals.length = 4;

console.log("After Decreasing:", animals.length, animals);

// example 5

const students = ["shambhu", "geet", "parth", "mayur", "somay"];

console.log("students length:", students.length);

console.log("Before:", students.length, students);

// Increase length
students.length = 8;

console.log("After Increasing:", students.length, students);

// Decrease length
students.length = 3;

console.log("After Decreasing:", students.length, students);


